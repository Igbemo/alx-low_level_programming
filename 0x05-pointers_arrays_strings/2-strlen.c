#include "main.h"

/**
 * _strlen - returns the length of the string
 * @t: input string to count
 * Description: returns the length of a given string
 * Return: length of string as int
 **/

int _strlen(char *t)
{
	int r = 0;

	while (t[r] != '\0')
	{

		r++;
	}
	return (r);
}
i
